VITE_API_URL=http://localhost:4000/api

# Optional — shows an admin-only "PricePilot" link in the sidebar once the
# separate PricePilot app (pricepilot/) is deployed. Leave unset to hide it.
# VITE_PRICEPILOT_URL=https://pricepilot.onrender.com
